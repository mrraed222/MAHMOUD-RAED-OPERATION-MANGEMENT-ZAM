# خطة معالجة أمان Supabase — حالة التنفيذ

**المشروع:** `uuphgpncmiwyigtqhjmd`
**تاريخ التحقق النهائي:** 19 أغسطس 2026
**النطاق:** `daily_reports` و`waste_logs` و`shift_issues` و`negative_reviews`، دوال الصلاحيات في schema `public`، وإعداد حماية كلمة المرور في Supabase Auth.

## الحالة التنفيذية

طبقت قاعدة البيانات معالجتي P0 وP1 من الخطة، ثم أُجري تحقق مستقل من منح تنفيذ الدوال وسياسات RLS. لم يُكسر الوصول التشغيلي المقصود: حافظ Admin وManager على الإدارة، وحافظ Owner على القراءة، بينما أصبح Supervisor محصوراً في بيانات فرعه. أما بند P2 المتعلق بكلمات المرور المسرّبة فلم يمكن إتمامه لأن منصة Supabase رفضت حفظ الإعداد في خطة المشروع الحالية.

| الأولوية | الإجراء | الحالة | التنفيذ/التحقق |
|---|---|---|---|
| P0 | سحب `EXECUTE` من `PUBLIC` و`anon` للدوال الخمس | **مكتمل** | migration: `harden_security_definer_function_execution` ثم `revoke_direct_anon_function_grants`. |
| P0 | عزل `rls_auto_enable()` و`prevent_profile_privilege_escalation()` عن أدوار العميل | **مكتمل** | لا يملك `anon` أو`authenticated` تنفيذ أي منهما مباشرة. |
| P1 | عزل عمليات Supervisor في التقارير والهدر والمشكلات والمراجعات حسب الفرع | **مكتمل** | migration: `isolate_supervisor_branch_access`؛ شروط الفرع موجودة في `USING` و`WITH CHECK`. |
| P1 | إبقاء Owner بقراءة فقط في الجداول المراجعة | **مكتمل** | سياسات Owner هي `SELECT` فقط على الجداول الأربعة. |
| P2 | تفعيل Leaked Password Protection | **محجوب بقيد الخطة** | واجهة Supabase أرجعت أن التكامل مع HaveIBeenPwned متاح لخطط Pro وما فوق. لم يُحفظ تغيير. |
| P2 | اختبار الصلاحيات قبل وبعد migrations | **مكتمل جزئياً** | تم فحص المنح والسياسات مباشرة؛ يلزم اختبار طرفي مستقبلي بحساب لكل دور. |

## تفاصيل الضبط المنفذ

### 1. تقليل الوصول إلى دوال `SECURITY DEFINER`

أُزيلت صلاحية التنفيذ العامة عن خمس دوال. يوضح فحص المنح التالي الحالة المطلوبة فعلياً:

| الدالة | `anon` | `authenticated` | سبب الإتاحة أو المنع |
|---|---:|---:|---|
| `current_user_branch_id()` | مرفوض | مسموح | دالة مساعدة لسياسات RLS المقيدة بالفرع. |
| `current_user_role()` | مرفوض | مسموح | دالة مساعدة لسياسات الدور. |
| `has_admin_permission(text)` | مرفوض | مسموح | تحقق صلاحيات للمستخدم الموثق. |
| `prevent_profile_privilege_escalation()` | مرفوض | مرفوض | دالة مرتبطة بـtrigger، ولا حاجة إلى RPC. |
| `rls_auto_enable()` | مرفوض | مرفوض | دالة event trigger إدارية، ولا حاجة إلى RPC. |

يحافظ هذا التصميم على الحد الأدنى اللازم من الصلاحيات، لكن سيبقى مستشار Supabase حاذراً من الدوال الثلاث المتاحة للمستخدمين الموثقين لأنها `SECURITY DEFINER`. لا يجوز سحب هذه الإتاحة مباشرة من دون اختبار، لأن سياسات RLS تستدعيها. تُستخدم RLS كطبقة دفاع في العمق، لكن المنح والسياسات يجب أن يتطابقا معاً [1].

### 2. عزل Supervisor على مستوى الفرع

تمت مراجعة السياسات المطبقة. يسمح Admin وManager بإدارة السجلات ضمن صلاحياتهما المعتمدة، ويملك Owner قراءة فقط. أما Supervisor، فيمر بفحص يربط `report_id` بالتقرير ثم يقارن `daily_reports.branch_id` مع `current_user_branch_id()`.

| الجدول | وصول Supervisor بعد المعالجة |
|---|---|
| `daily_reports` | قراءة تقارير الفرع المعيّن، وقراءة التقارير التي أرسلها، مع إدراج تقارير يرسلها بنفسه. |
| `waste_logs` | إدارة السجلات المرتبطة فقط بتقرير تابع لفرعه، في `USING` و`WITH CHECK`. |
| `shift_issues` | إدارة السجلات المرتبطة فقط بتقرير تابع لفرعه، في `USING` و`WITH CHECK`. |
| `negative_reviews` | إدارة السجلات المرتبطة فقط بتقرير تابع لفرعه، في `USING` و`WITH CHECK`. |

> يُقيّد `WITH CHECK` الصف الناتج في عمليات الإدراج والتعديل، ولذلك فهو ضروري مع `USING` لمنع تحويل سجل قائم أو سجل جديد إلى فرع خارج نطاق المستخدم [1].

## حماية كلمات المرور المسرّبة

تم فتح الإعداد الصحيح من المسار `Authentication → Sign In / Providers → Email` ومحاولة تفعيل **Prevent use of leaked passwords** ثم الحفظ. رفضت لوحة التحكم الحفظ برسالة مفادها أن حماية كلمات المرور المسرّبة عبر HaveIBeenPwned.org متاحة لخطط Pro وما فوق. أُلغي التغيير غير المحفوظ، وبذلك بقيت حالة Auth كما كانت.

تؤكد وثائق Supabase أن الميزة تستخدم Pwned Passwords، وأنها متاحة لخطة Pro وما فوق. كما توصي بأن لا يكون الحد الأدنى أقل من ثمانية أحرف، وأن تُستخدم متطلبات محارف قوية، وتدعم إعادة المصادقة وكلمة المرور الحالية عند التغيير [2]. هذه خيارات تخفيف مستقلة، لكنها **لم تُفعل** في هذه المهمة كي لا يتغير تدفق التطبيق من دون اختبار وظيفي.

## نتائج مستشار الأمان

أظهر الفحص النهائي أربع تحذيرات. لا توجد ملاحظة عن تنفيذ `anon` للدوال المراجعة بعد المعالجة.

| التحذير | التقييم | الإجراء التالي |
|---|---|---|
| `authenticated_security_definer_function_executable` لثلاث دوال مساعدة | مخاطرة متبقية مقصودة ومقيدة، وليست وصولاً عاماً | نقل الدوال إلى schema غير مكشوفة في بيئة اختبار، وتحديث سياسات RLS، ثم اختبار كل دور قبل دمج التغيير. |
| `auth_leaked_password_protection` | مفتوح بسبب قيد الخطة | عند توفر خطة مؤهلة، تفعيل الخيار من إعدادات Email ثم إعادة تشغيل مستشار الأمان. |

## قائمة اختبار التحقق الطرفي المقترحة

الفحص الحالي تحقق من التعريفات والسياسات مباشرة. قبل أي تغيير هيكلي جديد، يجب تنفيذ الحالات التالية باستخدام حسابات اختبار مستقلة؛ فالسياسة الخاطئة قد تفشل بصمت على هيئة صفوف مرشحة أو فارغة [1].

| الهوية | العملية | النتيجة المطلوبة |
|---|---|---|
| `anon` | استدعاء أي دالة من الدوال الخمس المراجعة | مرفوض. |
| Supervisor (فرع أ) | قراءة أو إدراج أو تعديل سجل هدر/مشكلة/مراجعة مرتبط بتقرير فرع أ | مسموح فقط عندما يطابق تقريره الفرع. |
| Supervisor (فرع أ) | قراءة أو تعديل أو حذف سجل مرتبط بتقرير فرع ب | مرفوض أو يعيد صفوفاً صفرية حسب العملية. |
| Owner | قراءة سجل تقرير أو هدر أو مشكلة أو مراجعة | مسموح. |
| Owner | إدراج أو تعديل أو حذف في الجداول المراجعة | مرفوض. |
| Admin وManager | تنفيذ مهام الإدارة المعتمدة | مسموح. |
| مستخدم Auth | اختيار كلمة مرور مسرّبة | يظل غير محمي حتى تتوفر الميزة في الخطة المؤهلة. |

## بنود خارج هذا التنفيذ

لم تشمل هذه migrations تعديل Edge Functions أو ملفات Storage أو طريقة تخزين/إرجاع رمز المرور. لذلك تبقى مراجعة `login-with-passcode`، تجزئة أي رموز مرور مخزنة، الحد من التخمين، سياسة `email_logs`، وخصوصية bucket `task-evidence` عناصر مفتوحة يجب التعامل معها في نطاق مستقل مع اختبار وظيفي.

## المراجع

[1]: https://supabase.com/docs/guides/database/postgres/row-level-security "Supabase Row Level Security"
[2]: https://supabase.com/docs/guides/auth/password-security "Supabase Password Security"
[3]: https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable "Supabase Database Security Advisor"
