# مراجعة أمان Supabase — نظام ZAM للعمليات

**تاريخ المراجعة الأولية:** 18 أغسطس 2026
**تاريخ التحقق اللاحق:** 19 أغسطس 2026
**النطاق:** سياسات RLS لجداول التقارير والهدر والمشكلات والمراجعات، منح تنفيذ دوال `SECURITY DEFINER`، وإعداد كلمة مرور Supabase Auth. لا يشمل هذا التحقق اختبار اختراق أو اختبار كل تدفق واجهة مستخدم بحسابات منفصلة.

## الملخص التنفيذي

تم تطبيق تحصينين فعليين على قاعدة البيانات: أُغلق تنفيذ الدوال الحساسة أمام `PUBLIC` و`anon`، وعُزل وصول المشرفين إلى سجلات الفرع الذي يتبعون له في جداول `daily_reports` و`waste_logs` و`shift_issues` و`negative_reviews`. أعاد فحص الصلاحيات تأكيد أن `anon` لا يستطيع تنفيذ أي من الدوال الخمس المراجعة، وأن الدالتين الإداريتين أو المرتبطتين بالمشغلات غير متاحتين كذلك لدور `authenticated`.

لا تزال حماية كلمات المرور المسرّبة **غير مفعّلة**. تمت محاولة حفظ الإعداد من لوحة Supabase، لكن المنصة رفضت الحفظ لأن الفحص عبر HaveIBeenPwned متاح لخطة Pro وما فوق. لم يُحفظ أي تغيير جزئي. وتبقى ثلاث ملاحظات في المستشار الأمني حول دوال مساعدة متاحة لـ`authenticated`؛ هذه صلاحية مقصودة حالياً لتشغيل سياسات RLS وليست إتاحة عامة. يجب التعامل معها كاستثناء موثق لا كتحذير مُغلق، إلى أن تُنقل الدوال إلى schema غير مكشوفة بعد اختبار كامل للتطبيق.

| المحور | الحالة | دليل التحقق في 19 أغسطس 2026 | الأثر العملي |
|---|---|---|---|
| تنفيذ دوال `SECURITY DEFINER` بواسطة `anon` أو `PUBLIC` | **مغلق** | `anon_can_execute = false` للدوال الخمس | يمنع استدعاء RPC غير الموثق للدوال المراجعة. |
| دوال `prevent_profile_privilege_escalation` و`rls_auto_enable` | **مغلق** | `authenticated_can_execute = false` للدالتين | لا يمكن استدعاؤهما مباشرة من أدوار العميل. |
| دوال تحديد الفرع والدور والصلاحية | **استثناء مقيد** | متاحة لـ`authenticated` فقط، وغير متاحة لـ`anon` | تدعم سياسات RLS الحالية؛ يلزم اختبار قبل أي نقل أو سحب للصلاحية. |
| عزل Supervisor حسب الفرع | **مغلق** | سياسات `USING` و`WITH CHECK` تربط `report_id` بفرع المستخدم الحالي | يمنع إدارة الهدر والمشكلات والمراجعات لسجلات فرع آخر. |
| حماية كلمات المرور المسرّبة | **مفتوح — قيد الخطة** | لوحة Supabase رفضت الحفظ بسبب متطلب Pro | الحسابات لا تستفيد من فحص كلمات المرور المسرّبة حالياً. |

## ما تم تطبيقه والتحقق منه

أُلغيت صلاحية `EXECUTE` عن `PUBLIC` و`anon` للدوال `current_user_branch_id` و`current_user_role` و`has_admin_permission` و`prevent_profile_privilege_escalation` و`rls_auto_enable`. وأكد الاستعلام اللاحق أن الدوال الثلاث الأولى ما زالت متاحة لـ`authenticated` فقط؛ وهذا مقصود لأنها تدخل في شروط RLS. أما الدالتان الإداريتان، فلا يستطيع أي من `anon` أو`authenticated` تنفيذهما مباشرة.

تم أيضاً استبدال وصول Supervisor الواسع بسياسات مقيدة بالفرع. في `daily_reports` تسمح السياسة بقراءة تقارير الفرع المعيّن أو التقارير التي أرسلها المستخدم نفسه. وفي `waste_logs` و`shift_issues` و`negative_reviews` تتحقق السياسة من وجود تقرير مرتبط يحمل `branch_id = current_user_branch_id()` في شرطي `USING` و`WITH CHECK`. الجمع بين الشرطين ضروري حتى يُمنع المستخدم من الوصول إلى سجل موجود في فرع آخر ومن إنشاء سجل جديد مرتبط به [2].

> لا يُعد إخفاء عناصر القائمة في الواجهة بديلاً عن هذه الطبقة. الضبط الفعلي للصلاحيات يقع في RLS ومنح قاعدة البيانات، إذ تتحقق PostgreSQL من المنح أولاً ثم من سياسة الصف [2].

## التحذيرات المتبقية والقرار الفني

يعرض مستشار الأمان أربع ملاحظات تحذيرية. ثلاث منها تتعلق بأن `authenticated` يستطيع تنفيذ الدوال الثلاث المساعدة المذكورة أعلاه وهي دوال `SECURITY DEFINER`. لا يُسمح لـ`anon` بتنفيذ أي منها، ولا تمنح الدوال الثلاث قدرة تعديل على البيانات؛ إلا أن إبقاءها في schema `public` يجعلها قابلة للاستدعاء عبر RPC للمستخدم الموثق. لا ينبغي إغلاق هذه الملاحظة بسحب صلاحية `authenticated` فوراً لأن سياسات RLS تعتمد عليها. البديل الآمن هو نقلها إلى schema غير مكشوفة وإعادة ربط السياسات في migration مستقلة مع اختبارات أدوار كاملة.

الملاحظة الرابعة هي تعطيل حماية كلمات المرور المسرّبة. توضح وثائق Supabase أن هذه الحماية تستخدم واجهة Pwned Passwords، وأنها متاحة لخطة Pro وما فوق [1]. لذلك لا يمكن تفعيلها في المشروع الحالي من دون تغيير خطة المشروع. كبدائل لا تتطلب هذا الخيار، توصي الوثائق برفع الحد الأدنى إلى ثمانية أحرف على الأقل وفرض مجموعات محارف قوية، كما تتيح طلب إعادة المصادقة وكلمة المرور الحالية قبل تغييرها [1]. لم تُفعّل هذه البدائل في هذه الجولة حتى لا يتغير سلوك تسجيل الدخول أو تغيير كلمة المرور من دون اختبار مخصص.

| التحذير المتبقي | القرار الحالي | الإجراء التالي الآمن |
|---|---|---|
| ثلاث دوال `SECURITY DEFINER` متاحة لـ`authenticated` | استثناء مقصود ومقيد، وليس وصولاً عاماً | بناء migration تجريبية تنقل الدوال إلى schema غير مكشوفة، ثم اختبار RLS لكل دور قبل الإنتاج. |
| Leaked Password Protection Disabled | لا يمكن حفظ التفعيل في الخطة الحالية | تغيير خطة Supabase ثم تفعيل الإعداد؛ أو اعتماد متطلبات كلمة مرور أقوى وإعادة المصادقة كتحسين مستقل. |

## بنود لا يثبت هذا التنفيذ إغلاقها

تظل النقاط التالية من المراجعة الأولية مفتوحة إلى أن تُراجع في الكود وبيئة اختبار منفصلة: عدم إعادة أي رمز مرور أو حقل حساس من دالة تسجيل الدخول، تخزين رمز المرور على هيئة hash قوي بدلاً من النص الصريح إن كان موجوداً، إضافة rate limit لمحاولات الدخول، مراجعة تحديثات `email_logs`، وتقييد الوصول إلى صور الأدلة في Storage. لم تتغير هذه البنود ضمن migrations الموثقة هنا، ولذلك لا يصح اعتبارها معالجة تلقائياً.

## معايير القبول التي تم فحصها

| السيناريو | النتيجة |
|---|---|
| `anon` يستدعي أي دالة من الدوال الخمس المراجعة | **مرفوض** وفق فحص منح التنفيذ. |
| `authenticated` يستدعي الدالتين الإداريتين/المشغلتين | **مرفوض** وفق فحص منح التنفيذ. |
| Supervisor يتعامل مع هدر أو مشكلة أو مراجعة مرتبطة بفرع آخر | **مرفوض بالسياسة** وفق شرطي `USING` و`WITH CHECK`. |
| Owner يقرأ السجلات ولا يديرها | **مضبوط بالسياسات** على الجداول المراجعة. |
| مستخدم يختار كلمة مرور مسرّبة | **غير محمي حالياً** بسبب قيد الخطة. |

## المراجع

[1]: https://supabase.com/docs/guides/auth/password-security "Supabase Password Security"
[2]: https://supabase.com/docs/guides/database/postgres/row-level-security "Supabase Row Level Security"
[3]: https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable "Supabase Database Security Advisor"
