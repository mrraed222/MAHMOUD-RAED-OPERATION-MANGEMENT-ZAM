# مراجعة أمان Supabase — نظام ZAM للعمليات

**تاريخ المراجعة:** 18 أغسطس 2026  
**نطاق المراجعة:** سياسات RLS، منح استدعاء الدوال، حماية تسجيل الدخول، وإعدادات المصادقة.  
**مهم:** هذه الوثيقة تسجل نتائج مراجعة للقراءة فقط. لا تطبق تغييرات قاعدة البيانات أو Edge Functions تلقائياً.

## الملخص التنفيذي

قاعدة البيانات تستخدم RLS في الجداول الأساسية، كما توجد سياسات مفيدة لعزل سجلات المشرف عن بقية الفروع ولمنع المستخدمين من إدارة الصلاحيات مباشرة. ومع ذلك، توجد مخاطر يجب معالجتها قبل التوسع في الاستخدام أو التعامل مع بيانات تشغيلية حساسة. الأولوية القصوى هي حماية رمز المرور ومنع أي استدعاء عام غير ضروري لدوال `SECURITY DEFINER`.

| الأولوية | النتيجة | الأثر | الإجراء المقترح |
|---|---|---|---|
| حرجة | رمز المرور مخزن كنص صريح، ووظيفة تسجيل الدخول تبحث عنه مباشرة ثم تعيد كائن `profile` كاملاً | تسريب الرمز أو استخدامه خارج النظام، وإتاحة التخمين المتكرر | استبدال الرمز المخزن بقيمة hash قوية؛ لا تُعد حقل الرمز في أي استجابة؛ وإضافة تحديد للمحاولات |
| حرجة | دوال صلاحيات `SECURITY DEFINER` قابلة للاستدعاء من `PUBLIC` و`anon` | سطح استدعاء أوسع من اللازم لدوال تستخدم في RLS | إلغاء `EXECUTE` من `PUBLIC` و`anon`، ومنحه للأدوار المطلوبة فقط بعد اختبار السياسات |
| عالية | سياسة تحديث `email_logs` تسمح لأي مستخدم موثق بتحديث أي سجل | إمكان تغيير حالة أو محتوى سجلات إرسال البريد | قصر التحديث على دالة خادم باستخدام `service_role`، أو على مالك/أدمن مع شرط صف واضح |
| عالية | صور الأدلة في Storage قابلة للعرض علناً | احتمال كشف صور تشغيلية أو مستندات داخلية | تحويل bucket `task-evidence` إلى خاص؛ منح القراءة للمصرح لهم فقط أو عبر signed URLs |
| متوسطة | حماية كلمات المرور المسرّبة معطلة | تقليل حماية الحسابات في حال استخدام كلمة مرور ضعيفة | تفعيل Leaked Password Protection في إعدادات Auth |
| متوسطة | بعض سياسات القراءة العامة مثل الفروع والقوالب مقصودة لتدفق التطبيق، لكنها متاحة لدور `public` | كشف معلومات تشغيلية محدودة لغير الموثقين | إن لم يكن التسجيل العام مطلوباً، قصرها على `authenticated` أو تقديمها عبر Edge Function محددة |

## ملاحظات مفيدة في الصلاحيات الحالية

- يظهر أن المشرف لا يقرأ إلا سجلات فرعه في `checklist_logs` وملفات موظفي فرعه في `profiles`.
- الوصول الشامل لتقارير اليوم ولملفات الموظفين محصور في سياسات المالك والأدمن بعد التوثيق.
- يوجد trigger باسم `trg_prevent_profile_privilege_escalation` على جدول `profiles`، وهي طبقة حماية مهمة ينبغي الحفاظ عليها عند تعديل سياسة `Users can update own profile`.
- إخفاء عناصر القائمة في الواجهة يساعد على تجربة الاستخدام، لكنه ليس بديلاً عن RLS. يجب أن يبقى فرض الصلاحيات في قاعدة البيانات وEdge Functions.

## خطة معالجة آمنة مقترحة

1. إنشاء بيئة فرع أو نسخة تطوير من Supabase قبل أي تعديل أمني.
2. تعديل Edge Function لتعيد حقول profile اللازمة فقط، من دون `passcode` أو حقول حساسة.
3. ترحيل رموز المرور الحالية إلى hashes باستخدام خوارزمية قوية؛ لا يُنشر hash إلى العميل.
4. إضافة rate limit ومدة منع مؤقتة لمحاولات تسجيل الدخول الفاشلة.
5. تقييد منح `EXECUTE` على دوال الصلاحيات، ثم اختبار كل دور من الأدوار التشغيلية والمالك والأدمن.
6. مراجعة سياسات `email_logs` وStorage، ثم اختبار إرسال البريد ورفع صورة إثبات ضمن حسابات اختبار.
7. تفعيل حماية كلمات المرور المسرّبة، ومراجعة التنبيهات الأمنية بعد كل migration.

## مراجع

- [Supabase Database Linter](https://supabase.com/docs/guides/database/database-linter)
- [Supabase Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [Supabase Password Security](https://supabase.com/docs/guides/auth/password-security)
- [Supabase Storage Access Control](https://supabase.com/docs/guides/storage/security/access-control)
