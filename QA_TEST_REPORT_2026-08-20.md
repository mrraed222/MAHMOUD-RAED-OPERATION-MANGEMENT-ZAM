# تقرير اختبار نظام ZAM

**التاريخ:** 20 أغسطس 2026  
**النطاق:** الواجهة الثابتة، المصادقة، الصلاحيات، التقارير، قوائم المهام، التحليلات، ووظائف Supabase المنشورة.  
**منفذ الاختبار:** Manus AI

## الملخص التنفيذي

أُجري اختبار وظيفي وأمني باستخدام حسابات اختبار معزولة لأدوار المالك، المدير، المدير التشغيلي، مشرفين في فرعين مختلفين، وباريستا. أثبتت الاختبارات صحة الفصل القرائي بين المشرفين حسب الفرع، عمل التقارير والتحليلات وسجل التقارير، ومنع المالك من تنفيذ التقرير اليومي. كما كشفت اختبار الكتابة عن ثغرة تسمح للمشرف بإنشاء تقرير في فرع آخر؛ تم إصلاحها بسياسة RLS محددة وإعادة اختبارها بنجاح. كذلك أزيل حقل رمز المرور من استجابة وظيفة الدخول، وأُصلح خطأ كان يمنع صفحة مهام المستخدم من تحديث ملفه وتشغيلها بصورة مستقرة.

| المجال | النتيجة | الملاحظة |
|---|---:|---|
| سلامة الواجهة | ناجح | فحص 19 صفحة إنتاجية؛ لا روابط محلية ثابتة مكسورة ولا تحذيرات RTL بعد الإصلاح. |
| جلسات الدخول | ناجح | أُصدرت جلسات اختبار صحيحة لجميع الأدوار، ورُفض رمز مرور غير صالح. |
| عزل المشرف حسب الفرع | ناجح بعد الإصلاح | حُجبت قراءة تقارير وهدر ومشكلات ومراجعات الفرع الآخر، ورُفض إدراج تقرير في فرع آخر بعد الترحيل. |
| عمليات الفرع المصرح بها | ناجح | أنشأ المشرف تقريراً في فرعه وسجلات الهدر والمشكلة والمراجعة التابعة له؛ وأنشأ الباريستا سجل مهمة في فرعه. |
| الواجهات الإدارية والتحليلية | ناجح | ظهرت بيانات الاختبار في لوحة التحكم والتحليلات وسجل التقارير وداشبورد الفرع. |
| تنظيف الاختبار | ناجح | لم يبق أي حساب أو تقرير أو سجل يحمل وسم الاختبار. |

## الإصلاحات المنفذة

### 1. منع إدراج تقرير مشرف في فرع غير مخصص له

أظهر اختبار أولي أن سياسة إدراج `daily_reports` كانت تتحقق من هوية المشرف في `submitted_by` فقط، ولذلك قبلت تقريراً موجهاً إلى فرع مختلف. يستبدل الترحيل `20260820115933_enforce_supervisor_report_insert_branch.sql` هذه السياسة بشرطين إضافيين: يجب أن يطابق `branch_id` فرع المستخدم الحالي، وأن يطابق `submitted_by` المستخدم الموثق. بعد النشر، أعادت محاولة الإدراج العابر للفرع استجابة `403` بسبب RLS، في حين نجح الإدراج الصحيح داخل فرع المشرف.

> هذه الضوابط تعتمد على RLS في قاعدة البيانات وليس على إخفاء عناصر الواجهة فقط، وهو الأسلوب المناسب لحماية البيانات متعددة الفروع. [1]

### 2. منع إعادة رمز المرور من وظيفة تسجيل الدخول

كانت وظيفة `login-with-passcode` تستخدم `select('*')` ثم تعيد ملف المستخدم كاملاً إلى المتصفح، بما يتضمن حقل `passcode`. تم تعديلها لاستخدام قائمة حقول صريحة وآمنة؛ لذلك تظل آلية إنشاء جلسة `magiclink` كما هي، لكن لا يصل رمز المرور المخزن إلى العميل في استجابة الوظيفة.

### 3. إصلاح صفحة «مهامي اليومية»

كانت الصفحة تحاول إعادة إسناد `currentUser` رغم أنه معرف كثابت في طبقة الجلسة، ما كان يسبب خطأ عند تحديث الملف الشخصي. تم استبدال إعادة الإسناد بـ`Object.assign(currentUser, freshProfile)`؛ وأظهر اختبار الباريستا بعد الإصلاح تحميل القوالب وتحديث حالة الإنجاز بنجاح.

### 4. اتساق لغة التسجيل واتجاهه

تم تصحيح صفحة التسجيل لتعلن `lang="ar"` و`dir="rtl"` صراحةً، بما يجعلها متسقة مع الواجهة العربية لبقية النظام.

## تغطية الاختبارات

| السيناريو | النتيجة النهائية |
|---|---|
| تسجيل الدخول بأدوار Owner وAdmin وManager وSupervisor (فرع أ/ب) وBarista | نجح؛ صدرت جلسة واكتملت قراءة الملف الشخصي الذاتي. |
| رمز مرور غير صحيح | رُفض باستجابة `401`. |
| قراءة المشرف لبيانات فرعه | ظهرت له بيانات فرعه فقط. |
| قراءة المشرف لبيانات فرع آخر | لم تظهر أي صفوف للتقرير والهدر والمشكلات والمراجعات. |
| إدراج مشرف لتقرير في فرع آخر | نجح قبل الإصلاح؛ رُفض بعد الإصلاح باستجابة `403`. |
| إدراج مشرف لتقرير وهدر ومشكلة ومراجعة في فرعه | نجح. |
| إدراج باريستا لسجل قائمة مهام في فرعه | نجح. |
| لوحة تحكم المالك | حملت بطاقات الفروع وإجماليات اليوم من بيانات الاختبار. |
| التحليلات | ظهرت أصناف الهدر التجريبية ووحداتها في العرض التحليلي. |
| سجل التقارير | ظهر تقريرا الاختبار الحقيقيان في السجل. |
| وصول المالك المباشر إلى التقرير اليومي | أُعيد توجيهه إلى لوحة المتابعة، وفق سياسة «إدارة/متابعة فقط». |
| صفحة مهام الباريستا | حملت المهام وفئاتها بعد الإصلاح. |

لم يُنفذ اختبار إرسال بريد إنتاجي، لأن سجلات توثيق نطاق Resend الخاصة بـZAM كانت محذوفة عند المراجعة. لا ينبغي اعتبار نجاح واجهة الإرسال دليلاً على وصول البريد حتى تُعاد سجلات DKIM وSPF وMX التي يعرضها Resend للنطاق.

## تنظيف بيانات الاختبار

استُخدمت بيانات وحسابات مميزة بوسم `QA-20260820` فقط. بعد الاختبار تم حذف **7** ملفات تعريف و**7** مستخدمين من Auth، بالإضافة إلى **4** تقارير اختبارية و**3** سجلات لكل من الهدر والمشكلات والمراجعات و**2** من سجلات قوائم المهام. أكد فحص لاحق أن عدد الصفوف المتبقية لجميع هذه الفئات يساوي صفراً.

## التحذيرات الأمنية المتبقية

يبقى مستشار Supabase الأمني عند أربعة تحذيرات. الثلاثة الأولى تخص الدوال المساعدة `current_user_branch_id` و`current_user_role` و`has_admin_permission`، وهي دوال `SECURITY DEFINER` متاحة عمداً لدور `authenticated` لكي تعمل سياسات RLS بدون كشف جدول الملفات الشخصية. تم منع `anon` من تنفيذها، ولا ينبغي إلغاء الوصول الموثق قبل نقلها إلى schema داخلي وإعادة تصميم جميع السياسات التي تعتمد عليها.

أما التحذير الرابع فهو تعطيل حماية كلمات المرور المسرّبة. محاولة تفعيلها من لوحة Supabase لم تحفظ لأن الميزة غير متاحة ضمن الخطة الحالية للمشروع. توثق Supabase أن هذه الحماية تتحقق من كلمات المرور المسرّبة عبر خدمة HaveIBeenPwned. [2]

> ما زال تخزين رموز المرور كنص قابل للقراءة في جدول `profiles` مخاطرة تصميمية تستحق مشروعاً مستقلاً: نقل التحقق إلى خادم موثوق باستخدام قيمة مجزأة، وإزالة عرض أو قراءة الرمز من استعلامات الواجهة، واستبدال تعديل الرمز بوظيفة إدارية محدودة الصلاحية. لم يُطبّق ذلك في هذا التحديث لتجنب كسر تدفقات إدارة الموظفين القائمة دون دورة ترحيل كاملة.

## الملفات المعدلة

| الملف | الغرض |
|---|---|
| `my-checklist/index.html` | إصلاح تحديث الجلسة في صفحة مهام المستخدم. |
| `register/index.html` | تعريف اللغة والاتجاه العربيين. |
| `supabase/functions/login-with-passcode/index.ts` | مصدر وظيفة الدخول المحصنة والمنشورة. |
| `supabase/migrations/20260820115933_enforce_supervisor_report_insert_branch.sql` | ترحيل منع كتابة تقرير مشرف خارج فرعه. |

## المراجع

[1]: https://supabase.com/docs/guides/database/postgres/row-level-security "Supabase: Row Level Security"
[2]: https://supabase.com/docs/guides/auth/password-security "Supabase: Password Security"
